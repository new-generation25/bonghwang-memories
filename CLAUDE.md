# 봉황 메모리즈 — 작업 규칙

김해 봉황동 오디오드라마 도보투어 PWA. EP.1 「아버지의 믹스테이프」.
Next.js 14 App Router · TypeScript · Tailwind · Firebase · 서비스워커.

두 장소(사무실·집)를 외장하드로 오가며 작업한다.

**세션을 열면 `docs/` 안의 가장 최근 `HANDOFF_<날짜>.md`를 먼저 읽어라.**
지금 어디까지 됐는지와 남은 일이 거기 있다.

**날짜 파일은 사용자가 "저장해라"라고 할 때 만든다.** 시키지 않았는데 미리
만들지 마라. 만들 때는 오늘 날짜로 새 파일을 열고, 지난 파일의 '남은 일'을
옮겨 적는다 — 끝난 것은 빼고, 왜 끝났는지 헷갈릴 만한 것은 맨 아래
'닫힌 항목'에 남긴다.

## 하지 말 것

- **`npm run build` 금지 · `.next` 삭제 금지 · 새 dev 서버 띄우기 금지.**
  3000 포트에 dev 서버가 돌고 있다. 빌드하면 그 서버가 쥐고 있던 청크가 전부
  404가 되고 하이드레이션이 죽는다 — "클릭해도 아무 반응 없음"의 정체다.
- 검증은 이 둘로 한다.
  ```bash
  npx tsc --noEmit
  npx next lint --file "src/app/track/[n]/TrackPageClient.tsx"
  ```
- `src/lib/buildVersion.ts`는 자동 생성이다. 손으로 고치지 마라.

## 말과 글

브랜드 금지어 — 소비자 화면에 남으면 안 된다.

| 쓰지 않는다 | 대신 |
|---|---|
| 봉황1988 | 봉황 메모리즈 |
| 아버지의 타임캡슐 | EP.1 「아버지의 믹스테이프」 |
| 약속의 기록자 | 기록자 |

대사 금지어는 `치매`(D6) · `미움`/`미워`(D6b). 평소엔 `npm run check:forbidden`이
빌드를 막지만 **여기서는 빌드를 못 돌린다 — 글을 고칠 때 눈으로 확인하라.**
검사만 따로 돌리는 것은 안전하다(`node scripts/check-forbidden.js`).

주석은 **한국어로, '무엇을'이 아니라 '왜'를 적는다.** 기존 파일들의 톤을 따를 것.
커밋 메시지도 같은 결이다 — `fix(오디오): 요소를 하나만 만들어 재사용`.

## 대본을 고칠 때

**[`docs/SCRIPT.md`](docs/SCRIPT.md)가 대사의 원본이다.** `src/lib/cues.ts`는
거기서 받아 적는 쪽이다 — 손으로 옮기지 마라.

```bash
npm run script:check    # 무엇이 바뀌는지 먼저 본다 (아무것도 안 건드림)
npm run script:bake     # 되쓰기 → 금지어 → 바뀐 줄만 굽기 → 길이 → sw 버전
```

- **`<br>`만 옮겼으면 굽지 않는다.** `generate-audio.mjs`가 걷어낸 뒤 해시를
  만들어서 캐시 키에 안 들어간다. 화면은 `subtitleLines.ts`가 그린다.
  TTS로 넘어가면 '비알'이라고 읽으므로 걷어내는 것이다.
- **줄을 나누거나 합치면 그 큐는 통째로 다시 굽는다.** 줄 하나가 TTS 한 조각이라
  줄 수가 곧 숨의 개수다. 글자가 같아도 나뉜 자리가 다르면 다른 음성이다.
  `script:bake`가 굽기 뒤 `fix-durations.mjs`로 `durationSec`을 다시 잰다.
- **큐를 새로 만들거나 지우는 것은 SCRIPT.md로 못 한다.** `trigger`·`next`·`ui`를
  대본이 표현하지 못한다 — `cues.ts`에서 먼저 만들고 `npm run script:dump`로
  대본을 따라오게 한다. 되쓰기는 큐 집합이 어긋나면 멈춘다.
- 같은 글자인데 목소리가 마음에 안 들면 `/debug/script`에서 **줄별로** 굽고 저장한다.
- `script:bake`는 ffmpeg와 `TYPECAST_API_KEY`가 필요하다. 없는 자리에서는
  `npm run script:apply`까지만 돌린다(되쓰기 + 금지어 검사).
- Typecast는 같은 설정으로 구워도 결과가 매번 다르다. 마음에 드는 것이 나오면
  그 파일을 남겨라. (API 제약 상세는 `docs/HANDOFF_2026-07-23.md` §2)

## 핵심 제약 (구현명세서 D-규칙)

| | |
|---|---|
| D1·D2 | 채널은 tape(아버지·과거) / call(소영·현재) / shop(가게 증언). 소영은 현장에 없고 통화로만 동행한다 |
| D3 | 자동 발신 금지. 쪽지 번호를 보고 참여자가 직접 [전화 걸기] |
| D4 | 실물 QR 정확히 6회 = 인트로 1 + 거점 5. `validateCueGraph()`가 검사한다 |
| D6·D6b | 병명·미움 표현 금지 (위 '말과 글') |
| D7 | 존댓말→반말 전환은 Track 1 종료 후 한 번. 되돌리지 않는다 |
| D8 | 음성 전량 선다운로드. 파일을 늘리면 캐시 대상과 sw 버전이 함께 는다 |
| D9 | 스킵은 재생 15초 후. 다시듣기 무제한. **시간 기반 자동재생 금지** — GPS도 재생 트리거가 아니다 |
| D10 | B면 해제는 J-카드 소원 3개 이상 — 트랙 1~4 완료에서 파생, 별도 상태 없음 (1미션 실패 허용) |
| D11 | 능소화 AR은 정적 프레임 폴백이 기본 경로다 |
| D12 | 사진·녹음은 기기 저장이 기본. 서버 업로드는 명시 동의 시에만 |
| D13 | 재생 단위는 '번들'. 짧은 컷을 나열하지 않는다 |

## 저장소에 없는 것

`.env.local`(Firebase 6개 · 네이버 지도 · `TYPECAST_API_KEY`)과 아래는 gitignore라
**외장하드를 따라다닌다. 클론만으로는 굴러가지 않는다.**

```
public/audio/_raw/     원본 녹음 · TTS 후보 · 굽기 결과
tts-out/               일회성 TTS 산출물
scripts/tts-once.mjs   API 키를 다루는 도구
scripts/voices.json    보이스 설정 (voices.example.json이 템플릿)
.audio-cache/          줄 단위 굽기 캐시 — 안 바뀐 줄은 API를 다시 부르지 않는다
```

채택된 것만 `public/audio/`·`public/images/`로 옮겨 커밋한다.

## 문서 지도

| | |
|---|---|
| `docs/HANDOFF_<날짜>.md` | **가장 최근 것이 현재 상태.** 세션 처음에 읽고 끝에 적는다 |
| `docs/SCRIPT.md` | **대사의 원본.** 배경·연표·인물 + 대본 전문. 안 정한 칸과 어긋난 곳도 여기 |
| `docs/FLOW.md` | 랜딩부터 피날레까지 전체 진행 흐름 |
| `docs/HANDOFF_*.md` | 아카이브. "왜 그렇게 했나"를 캘 때만 연다 |
